// src/Features/LoadRecordFromSaved/backend.as

// make sure to save the record with some metadata so that it can still be used in "MLHook", we need the player id and the map url/id, (we also have to check if the score of the record is the same as the one in the saved file)

namespace Features {
namespace LRFromSaved {

}
}