// src/Features/CurrentRecordsInfo/ui.as

namespace Features {
namespace CRInfo {

    // Copy the structure of how g++ shows loaded records
        // Include more of the valable information for each record in a tree-style dropdown (add what might be relevant for identification here)
        // This dropdown should include 
            // - The ghost's Country
            // - The ghost's ID (value, MwId and IdName)
            // - The ghost's nickname
            // - the actual image from 'OverrideZoneIcon_ImageUrl'
            // - The ghost's trigram
            // -- and everything relevant in "Results"
        // Include a way to save the record to a file
    // Include a way to save the validation replay to a file
    
    // Add a section at the top that has
    // - A button to remove all records
    // mm

}
}